feat: 替我行动执行停止 + 悬浮球/聊天语音输入（第十二阶段）

【执行停止机制】
- 新增 ActionRunController：独立管理轮次推进与停止标志，可单测
- FloatingProgressOverlay 显示"停止执行"按钮，点击后通知 ViewModel
- executePendingAction/continueDecision 在每个关键点检查 isCancelled 提前终止
- UI 在 Thinking/Executing/PendingConfirm 状态显示停止按钮
- 停止后记为 cancelled 状态并写入日志，保留已执行动作摘要

【语音输入】
- 新增 VoiceInputController：封装 SpeechRecognizer，返回识别结果或错误
- 悬浮面板（floating_panel.xml）增加语音按钮，识别结果填入输入框
- 聊天界面麦克风按钮接通真实识别（修复第七阶段遗留的空壳实现）
- Manifest 补齐 RECORD_AUDIO 权限与语音识别服务 query 声明
- 悬浮窗语音失败时提示用户到主界面授权（Service 无 Activity 上下文）

【改进与修复】
- 进度计数跨轮重置，避免显示"3 / 2"之类错乱
- voiceInputMode 从被动标记改为主动识别流程（startVoiceInput/cancelVoiceInput）
- BatchedLogWriter 状态映射补充 PendingConfirm.Canceled → "cancelled"
- 移除 FloatingProgressOverlay 无用字段与 TODO 代码（isPaused/layout/statsContainer）

【单元测试】
- 新增 ActionRunControllerTest：10 个测试覆盖启动/停止/轮次推进/边界条件

【文件清单】
新增：ActionRunController.kt, VoiceInputController.kt, ActionRunControllerTest.kt
修改：ChatViewModel.kt, FloatingProgressOverlay.kt, FloatingWindowService.kt,
      AgentApp.kt, floating_panel.xml, AndroidManifest.xml, BatchedLogWriter.kt
