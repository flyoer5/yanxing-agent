fix: 主线程阻塞 + Release 混淆开启（第十三阶段）

【主线程阻塞修复】
- ActionExecutor.click/longPress/inputText/textInput 改为协程 suspend 函数
- Thread.sleep(50) / RETRY_DELAY_MS → kotlinx.coroutines.delay（可被取消）
- withRetrySupport 和 withService 也改 suspend，保持链式调用一致
- ChatViewModel.executePendingAction 调用时加 withContext(Dispatchers.Default)，避免阻塞主线程导致 ANR

【Release 混淆】
- build.gradle.kts Release 构建开启 isMinifyEnabled=true + isShrinkResources=true
- proguard-rules.pro 补全所有依赖规则：Hilt、Room、OkHttp、Kotlin Serialization、Compose、AccessibilityService、data/Network DTOs
- 预计包体积缩小 30-35%（Debug 19MB → Release 12-13MB）

【单元测试】
- 新增 ActionExecutorCoroutineTest：验证 delay 可被取消、重试逻辑在取消时提前退出、成功操作不受影响（5 个测试用例）

【文件清单】
新增：ActionExecutorCoroutineTest.kt
修改：ActionExecutor.kt, ChatViewModel.kt, app/build.gradle.kts, app/proguard-rules.pro, progress.md
